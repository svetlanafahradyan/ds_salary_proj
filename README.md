# ds_salary_proj


## Limitations

Glassdoor actively restricts automated scraping — the "Load more" button is frequently suppressed or rendered non-functional for bot-like sessions, meaning the scraper may return far fewer than `num_jobs` results even when more listings exist on the site. 
